/**
 * 
 * @param {String} name 
 * @param {Number} age 
 * @param {String} job
 * @param {Boolean} isAdmin
 * @returns 
 */

function createUser(name, age, job, isAdmin) {
    return {name, age, job, isAdmin}
}